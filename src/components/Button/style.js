import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  buttonStyles: {
    backgroundColor: '#7804C6',
    width: 302,
    marginTop: 20,
    borderRadius: 10,
    height: 58,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export {styles};
